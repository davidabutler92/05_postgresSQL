const pool = require('../utils/pool');

module.exports = class Pokemon {
  id;
  name;
  type;

  constructor(row) {
    this.id = row.id;
    this.name = row.name;
    this.type = row.type;
  }

};

